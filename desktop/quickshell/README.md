# STRATAGEM DARK shell prototype boundary

Future Quickshell module: workspace list, clock, audio/network state, tool launcher
and profile indicator. Use `branding/tokens.json` and expose keyboard access. Keep
model/adapters separate from presentation and no privileged package commands in QML.
Quickshell is not yet in the install selection and no working panel is claimed.
