# Packaging boundary

Planned pacman packages: `stratagem-dark-cli`, `stratagem-dark-desktop`,
`stratagem-dark-core`, and profile metapackages. Keep CLI/runtime, system defaults,
and user customization separate. Build in a clean Arch environment and sign outputs.
No PKGBUILD is released until the layout and source archive checksums are fixed.
