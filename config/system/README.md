# System defaults

Reserve this directory for minimal reviewed drop-ins. Initial scaffold enables no
services and writes no host files. Do not replace pacman.conf, sudoers, network,
bootloader or os-release files wholesale. Prefer packaged includes and explicit policy.
